name: test
description: test

runs:
  steps:
    - shell: bash
      name: Echo
      env:
        MESSAGE: TEST SUCCESS
      run: |
        echo $MESSAGE > test.log
    # - name: upload
    #   uses: actions/upload-artifact@v2
    #   with:
    #     name: test-log-file
    #     path: test.log